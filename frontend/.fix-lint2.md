## 修剩余 2 个 Bug

### 1. StreamText.jsx 仍有 2 个 lint error
文件: frontend/src/components/StreamText.jsx
问题: npm run lint 报告 2 errors（react-hooks/set-state-in-effect 在行19-20）
修改: 彻底重构 useStream，消除所有 lint error。用 useRef 管理 timer，不在 effect 中同步 setState

### 2. JobDetail.jsx 用了假候选 ID  
文件: frontend/src/pages/JobDetail.jsx
问题: `'demo-candidate-001'` 不是真实 ID
修改: 和 CandidateProfile 一致，使用 fetchCandidates 获取真实 ID

### 验证
- npm run lint 零 error
- npm run build 成功
- pytest 306 passed
