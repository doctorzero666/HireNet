## 任务：RoleSelect + 分析页（Home/AnalysisChat/AnalysisReport）完全照搬 Island 版视觉，保留 API 连通

项目：/Users/zhichaojiang/Documents/HireNet/frontend/

### Island 版完整 CSS 在 island-ref.html 中，请先读取该文件提取所有关键 class。

### Island 版组件代码参考（在 /tmp/island_script_*.jsx 中），关键的：

**RoleSelect**：guild-logo (50px/900/text-shadow), Ribbon (swallowtail飘带), role-card (bg var(--bg-content), radius 20px, padding 28px, hover border app-teal + translateY(-4px)), btn btn-primary btn-lg btn-block pill
**Home**：eyebrow, composer textarea (min-height 74px, radius 18px), chip examples, composer-bar with btn btn-primary
**AnalysisChat**：msg/avatar/bubble (AI bg #19c8b9/#fff, border-radius 18px/6px不对称), quick-opts pill buttons
**AnalysisReport**：report-head h1 (27px/900), metric (rgba white 0.6 + border 1.5px #e8dcc8), section-h

### 改造规则

**RoleSelect.jsx**：
- 完全替换为 Island 的设计：guild-logo标题 + Ribbon飘带 + 两张role-card
- 雇主按钮 navigate('/employer/hub')，求职者 navigate('/jobseeker')
- 使用 Ribbon 组件（从 Island shared.jsx 复制过来，改写为 React 组件）
- role-card hover：border-color #82d5bb + translateY(-4px)

**EmployerHome.jsx**：
- 使用 Island 的 composer 设计：textarea + chip 示例标签 + btn btn-primary
- 保留我们现有的 startAnalysis API 调用逻辑
- textarea placeholder 和 focus 样式对齐 Island

**AnalysisChat.jsx**：
- 使用 Island 的 msg + avatar + bubble 设计（AI 气泡 bg #19c8b9 #fff，不对称圆角）
- 保留我们现有的 API 对话逻辑（replyAnalysis + runDecision）
- 快捷选项用 Island 的 quick-opt pill 样式
- AI 消息用 StreamText 打字效果（可从 Island shared.jsx 复制 useStream hook）

**AnalysisReport.jsx**：
- 使用 Island 的 report-head + metric + section-h 设计
- 保留我们现有的数据驱动逻辑（result.decisions, result.tasks, decisionByTaskId）
- metric 卡片：bg rgba(255,255,255,0.6), border 1.5px solid #e8dcc8, radius 20px, padding 16px 18px
- TaskCard 按 Island 风格（卡片 bg rgba white + border soft）

### 共享组件
- 把 Island 的 Ribbon 组件加到 src/components/Ribbon.jsx
- 把 Island 的 Icon 组件加到 src/components/Icon.jsx

### 约束
- 保留所有现有 API 调用逻辑（startAnalysis, replyAnalysis, runDecision）
- 保留 react-router-dom 导航方式
- 只改内联 style 和组件结构，不改 API 签名
- npm run build 必须成功
