## 任务：RoleSelect 和 Analysis 页面完全照搬 Island 版设计

项目：/Users/zhichaojiang/Documents/HireNet/frontend/

### Island CSS 规范（从原型 CSS 提取，逐一对应）

**RoleSelect 页面：**
- `.role-card`: flex column, bg var(--bg-content) #f7f3df, radius 20px, padding 28px, shadow elev-sm, border 2px solid transparent, transition 0.3s ease
- `.role-card:hover`: translateY(-4px), box-shadow 0 8px 24px rgba(114,93,66,0.15), border-color var(--app-teal) #82d5bb
- `.guild-logo`: font-size 50px, weight 900, color var(--text) #794f27, text-shadow 0px 3px 0px rgba(189,174,160,0.55), letter-spacing 0.01em
- `.role-tag`: pill tag, font-size 12px weight 700, padding 4px 12px, border 2px solid #e8dcc8
- `.role-switch`: pill button, font-size 12.5px weight 700, color var(--text), padding 6px 14px, radius pill, border 2px solid #e8dcc8
- `.btn`: 45px height, padding 0 20px, radius pill 50px, font-size 14px weight 600, letter-spacing 0.02em, transition 0.25s ease
- `.btn-soft`: color #fff, bg #19c8b9, border-color #19c8b9, box-shadow 0 5px 0 0 #11a89b
- `.btn-primary`: color #794f27, bg #f8f8f0, border-color #f8f8f0, box-shadow 0 5px 0 0 #bdaea0
- `.btn-lg`: height 48px, padding 0 32px, font-size 16px, border-radius 24px
- `.rs-board-title`: text-align center, margin-bottom 24px

**Analysis 页面（EmployerHome + AnalysisChat + AnalysisReport）：**
- `.report`: max-width 820px, margin 10px auto 22px, padding 10px 0 120px
- `.report-head`: margin-bottom 20px. h1: font-size 27px, weight 900, color #794f27
- `.report-summary`: flex wrap, gap 10px, margin-bottom 20px
- `.bubble`: font-size 14px, line-height 1.65, padding 13px 18px, background #f7f3df, radius 18px, border-top-left-radius 6px (user), box-shadow elev-sm
- `.bubble.self`: background #19c8b9, color #fff, radius 18px, border-top-right-radius 6px
- `.quick-opts`: flex wrap, gap 8px, margin-top 12px
- `.quick-opt`: font-size 13px weight 700, height 38px, padding 0 16px, radius pill, border 2px solid #e8dcc8, color #725d42
- `.input-text` (single-line): height 44px, padding 0 18px, radius pill, border 2.5px solid #c4b89e
- `.input-text` (multi-line): min-height 74px, radius 18px, padding 11px 16px, border 2.5px solid #c4b89e
- `.task-card`: card style with agent info
- `.metric`: padding 16px 18px, bg rgba(255,255,255,0.6), radius 20px, border 1.5px solid #e8dcc8
- `.metric-grid`: 4-col grid, gap 14px
- `.section-h`: flex space-between, margin 30px 0 14px
- 输入框 focus: border-color #ffcc00, box-shadow 0 0 0 3px rgba(255,204,0,0.15)

### 改造页面

**Roleselect.jsx** — 完全按 Island 的 guild-logo、role-card、role-tag、btn-soft、role-switch 样式重写：
- 标题 #794f27, weight 900, text-shadow 0 3px 0 rgba(189,174,160,0.55)
- 两张角色卡片：bg #f7f3df, radius 20px, padding 28px, border 2px transparent, hover border #82d5bb + translateY(-4px)
- 按钮 pill, 45px height

**EmployerHome.jsx** — 按 btn-soft + input-text + btn 样式：
- 输入框 radius 50px pill, height 48px, border 2.5px #c4b89e
- 示例标签 pill, bg #f0e8d8
- 按钮 btn-soft 青绿 3D

**AnalysisChat.jsx** — 按 bubble + quick-opts 样式：
- 用户气泡：bg #19c8b9, color #fff, radius 18px, border-top-right-radius 6px
- AI气泡：bg #f7f3df, color #725d42, radius 18px, border-top-left-radius 6px
- 快捷选项：pill, border 2px #e8dcc8, height 38px

**AnalysisReport.jsx** — 按 report + metric-grid + section-h 样式：
- 汇总栏：bg rgba(255,255,255,0.6), border 1.5px #e8dcc8, radius 20px
- Stat卡片：同 metric

### 约束
- 只改内联 style 和 CSS
- 零像素残留
- npm run build 成功
- 参照文件：/Users/zhichaojiang/Documents/HireNet/frontend/island-ref.html
